@import "common/var.styl"

.g-popover
	display inline-block

.g-popover-wrapper
	font-size $font-size
	background-color $background-color
	color $color
	box-shadow $box-shadow
	line-height 1
	max-width 300px
	max-height 100vh
	border-radius 3px
	padding 10px
	margin-top 5px
